[project]
name = "com-risk-runtime"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["torch>=2.6,<3", "numpy>=2,<3", "pydantic>=2,<3"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/com_risk_runtime"]
