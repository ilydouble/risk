# 前端实施状态

早期页面计划已落实为工作台界面，并按 FSD 整理到 `src/app`、`src/pages`、`src/features`、`src/entities`、`src/shared`。当前技术栈和启动方式以仓库根 [README](../README.md) 为准；分层与 API 约定见 [前端架构](../docs/architecture/frontend.md)。

## 已接通后端

- 登录、退出、会话恢复。
- 企业检索、画像、2–3 跳图谱、评分演示快照。
- 画像页文件预签名上传、确认、列表与下载。
- 后端 OpenAPI 生成 TypeScript DTO，SDK 统一 HTTP 信封与错误码。

## 仍是演示逻辑

- 首页汇总、预置案例、报告、授信决策、模型档案/评估看板、批量评估。
- 评分、SHAP 解释及图谱事实来自后端种子快照；没有在线模型或正式外部数据源。

后续接真实数据或模型时，应分别定义数据来源、权限、更新频率和验收证据，再替换对应模块；不要把当前快照结果当成生产评级依据。
